Channel List:
+ buddy check
+ focus on text line on switch
+ implement filtering
+ on reload don't disable whole widget, just reload button
+ disable stop by default
+ join on double click
+ s/m_refreshButton/m_reloadButton
- implement stop button
- apply on return press in search field
- save config/reload on switch
- non-auto column resizing
- richtext tooltips of topics
- highlight tab on data

Character chart:
- designerize
- detect font of textview
- use a container widget
- add a insert button
- react on double click

URL Grabber:
- designerize
- full width listview
- implement save
- implement clear
- update URLs
- maintain internal URLList

Server List:
- 




// old stuff
if nomenubar, put it in first of popupmenu then seperator
set sess->gui->win to new detached window renable that shit by setting sess->gui->win.
textwidget: if doublebuffer, don't resize if smaller than current size 
mainwindow: go from list of mainviews to count of mainviews (like app from mainwindows to count)
fix quit with multiple windows--- implement kmail-like passive quit
in getstr- add pixmaps to buttons and right justifyt.
fix cmd complettion
port getstr/getint to kdialogbase
use namespaces
repalce fcn(void) with fcn()
